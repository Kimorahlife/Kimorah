# Copy to .env.development.local (dev) or set in Render (prod).
JWT_SECRET=
MONGO_URI=
NODE_ENV=
PORT=3001
ORIGIN_URL=http://localhost:5173

# Claude. Powers the curriculum auto-translator (Spanish → English).
# Without it the app still runs; the Translate button returns 503.
ANTHROPIC_API_KEY=
